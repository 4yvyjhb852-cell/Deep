# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Deep Sanctuary") is currently an empty project with only a `README.md`. There is no application code, no dependency files, no services, no tests, and no build system.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `go.mod`, or similar files exist.
- **No services to run** — no backend, frontend, or database services are configured.
- **No tests or lint checks** — no test framework or linter is configured.

When application code is added in the future, update this file with relevant setup, run, test, and lint instructions.
